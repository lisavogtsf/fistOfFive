<!-- _poll_preview.html.erb -->
<!-- given from template:  locals: {user: User.find_by_id(poll.user_id), poll: poll -->
<!-- show poll topic, with link, date and number of replies -->
<!-- poll -->
<!-- 
courses is working for now -->

<% if path == "user" %>
	<%= link_to poll.topic, user_poll_path(poll.user_id, poll.id)  %>
<% else %>
	<%= link_to poll.topic, course_poll_path(poll.course_id, poll.id)  %>
<% end %>

