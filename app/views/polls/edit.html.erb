<!-- edit.html.erb -->
<% @page_title = "Edit Poll" %>
<%= render partial: "users/user_navbar", locals: {user: @user, poll: @poll, page_title: "Edit Poll"} %>

<div class="jumbotron">
		<%= render partial: "poll_form", locals: {user: @user, form_title: "Edit Poll", button_label: "Save Changes"}   %>
		<%= button_to "Cancel", user_path(@current_user.id), method: :get, class: "btn btn-form btn-danger" %>
</div>