<!-- _student_form.html.erb -->
<div class="row">
	<%= form_for @student, html: {class: "form-horizontal"} do |f|  %>
	<fieldset>
		<legend><%= form_title  %></legend>
		<div class="form-group">
			<%= f.text_field :first_name, class: "form-control", placeholder: "First name", autofocus: true  %>
		</div>	<!-- closes form-group -->
		<div class="form-group">
			<%= f.text_field :last_name, class: "form-control", placeholder: "Last name"  %>				
		</div>	<!-- closes form-group -->
		<div class="form-group">
			<%= f.text_field :sms_phone_number, class: "form-control", placeholder: "Phone Number"  %>
		</div>	<!-- closes form-group -->
		<div class="form-group">
			<%= f.submit button_label, class: "form-control" %>
		</div>	<!-- closes form-group -->
	
	</fieldset>
	<% end %><!--  ends form for -->
</div>