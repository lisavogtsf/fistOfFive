<!-- _course_preview.html.erb -->
<!-- just shows course title and has a link to that course's detail page -->
<div class="row">
	<p><%= course.id %> <%= link_to course.title.titleize, user_course_path(user.id, course.id) %></p>
</div>
